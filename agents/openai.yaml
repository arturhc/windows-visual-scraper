interface:
  display_name: "Windows Visual Image Scraper"
  short_description: "Collect social images and build visual reports"
  default_prompt: "Use $windows-visual-image-scraper to collect images from these Facebook or Instagram profiles and deliver a structured folder with a Markdown report."
policy:
  allow_implicit_invocation: true
