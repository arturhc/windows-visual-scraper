interface:
  display_name: "Creative Content Intelligence"
  short_description: "Mine and analyze creative image and video references"
  default_prompt: "Use $windows-visual-image-scraper to turn these brand sources into a structured, traceable creative-reference knowledge base."
policy:
  allow_implicit_invocation: true
